import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fortytwo.bus',
  appName: '42',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
